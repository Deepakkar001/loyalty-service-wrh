# LoyaltyOS optional infrastructure (Kafka only — when enabled below).
#
# Redis is NOT run via Docker for this project. Use a local Redis instance on
# localhost:6379 (see backend/README.md). Spring connects via spring.data.redis
# in application.yml (defaults: REDIS_HOST=localhost, REDIS_PORT=6379).

services:

  # --- Kafka stack (commented out until we need it; app runs without a broker) ---
  # kafka:
  #   image: confluentinc/cp-kafka:7.7.0
  #   container_name: loyaltyos-kafka
  #   ports:
  #     - "9092:9092"
  #   environment:
  #     KAFKA_NODE_ID: 1
  #     KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
  #     KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
  #     KAFKA_LISTENERS: PLAINTEXT://kafka:29092,CONTROLLER://kafka:29093,PLAINTEXT_HOST://0.0.0.0:9092
  #     KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
  #     KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
  #     KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:29093
  #     KAFKA_PROCESS_ROLES: broker,controller
  #     KAFKA_AUTO_CREATE_TOPICS_ENABLE: "false"
  #     KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
  #     KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
  #     KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
  #     CLUSTER_ID: "MkU3OEVBNTcwNTJENDM2Qk"
  #   healthcheck:
  #     test: ["CMD", "kafka-broker-api-versions", "--bootstrap-server", "localhost:9092"]
  #     interval: 15s
  #     timeout: 10s
  #     retries: 10
  #
  # kafka-ui:
  #   image: provectuslabs/kafka-ui:latest
  #   container_name: loyaltyos-kafka-ui
  #   ports:
  #     - "8090:8080"
  #   environment:
  #     KAFKA_CLUSTERS_0_NAME: local
  #     KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:29092
  #   depends_on:
  #     kafka:
  #       condition: service_healthy
