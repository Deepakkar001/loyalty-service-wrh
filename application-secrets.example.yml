spring:
  mail:
    # Example only. Put real values in `application-secrets.yml` (ignored by git).
    username: "your-email@gmail.com"
    password: "your-app-password"

app:
  jwt:
    # Local dev only. Use 32+ chars for HS256.
    secret: "change-me-to-a-long-random-secret-at-least-32-chars"

